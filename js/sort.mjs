const onlyImage = document.querySelector("#onlyImages")

export function sortOnlyImage() { 
    onlyImage.addEventListener("click", () => {
        let POST_HAS_IMAGE = true
        let POST_HAS_RECOM = false
})
}